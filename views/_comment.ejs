<li>
    <p>
        <% if(locals.user && locals.user.id == comment.user.id){ %>
            <small>
                <a href="/comments/destroy/<%= comment.id %>">X</a>
            </small>
        <% } %>
        COMMENT: <%= comment.content %>
        <br>
        <small>
           NAME: <%= comment.user.name %>
        </small>
        <!-- :: -->
        <small>
            <% if(locals.user){ %>
                <a class="toggle-like-button" data-likes="<%= comment.likes.length %>" href="/likes/toggle/?id=<%= comment._id %>&type=Post">
                    <%= comment.likes.length %> Likes
                </a>

            <% }else{ %>
                <%= comment.likes.length %> Likes
            <% } %>

        </small>
    </p>
</li>